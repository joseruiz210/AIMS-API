services:
  # ─── BACKEND API (Node.js / Express / Prisma) ───────────────────
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aims-backend
    restart: always
    command: npm run dev
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=development
      - FRONTEND_URL=http://localhost:8081
      - DATABASE_URL=postgresql://neondb_owner:npg_h7dN8kQOgqxT@ep-floral-heart-acnwfivw-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
      - JWT_SECRET=your_jwt_secret_key_change_in_production
      - JWT_EXPIRES_IN=24h
      - GOOGLE_CLIENT_ID=801203695881-vjkbm79n28utn02fkiei3tmrieqmkd37.apps.googleusercontent.com
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_USER=juanes15342@gmail.com
      - SMTP_PASS=vgaodeyjpneqilyr
      - EMAIL_FROM=AIMS API <juanes15342@gmail.com>
      - GEMINI_API_KEY=${GEMINI_API_KEY:-AQ.Ab8RN6IHLoF13R1c75QX5BEU9ySQRcm0m5iQSlv-XizmWM9mbw}
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-3.5-flash-lite}
    dns:
      - 8.8.8.8
      - 1.1.1.1
    volumes:
      - .:/app
      - /app/node_modules

  # ─── FRONTEND (Expo / React Native Web) ──────────────────────────
  frontend:
    build:
      context: ./frontend/MiProyecto/MiProyecto
      dockerfile: Dockerfile
    container_name: aims-frontend
    restart: always
    ports:
      - "8081:8081"
    environment:
      - EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
      - EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=801203695881-vjkbm79n28utn02fkiei3tmrieqmkd37.apps.googleusercontent.com
      - EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=801203695881-gv5vvikcfpbjkpvthpqd1babkk512b4h.apps.googleusercontent.com
    volumes:
      - ./frontend/MiProyecto/MiProyecto:/app
      - /app/node_modules
    depends_on:
      - backend
